# @g-loot/react-tournament-brackets declares a React 18 peer dependency but
# works correctly on React 19. Without this, `npm install` / `npm ci` abort
# with ERESOLVE. Committed so local dev, CI, and the Docker image build all
# resolve dependencies identically.
legacy-peer-deps=true
