package hr.mrodek.apps.futsal_turniri.enums;

/**
 * Playing surface the tournament is held on. Purely descriptive (shown as a
 * coloured stat tile on the detail page) - does not affect scheduling, format
 * or any other logic.
 */
public enum Surface {
    TRAVA,
    UMJETNA_TRAVA,
    ASFALT,
    DVORANA
}
